/* 
? :
Serve como if's menores 
*/
const pontuacaoUsuario = 999;
const nivelUsuario = pontuacaoUsuario >= 1000 ? 'Usuário Vip' : 'Usuário Normal';
/* 
    ? - funciona como if
    : - funciona como else
*/

/*if(pontuacaoUsuario >= 1000)
    console.log('Usuario vip');
else
    console.log('Usuário normal')*/